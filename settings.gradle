include ':app'

rootProject.name = 'JavaTools'

def libs = []

new File("gradle/config/libs.txt").eachLine {
    libs << it.trim()
}

libs.forEach { lib ->
    include(":$lib")
    project(":$lib").projectDir = new File(settingsDir, "./library/$lib")
}