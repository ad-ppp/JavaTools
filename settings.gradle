include ':app'

rootProject.name = 'JavaTools'

def libs = [
    'jar-searcher',
    'base-tool-lib',
    'asm-tools',
    'apk-tool',
    'jenkins-tools'
]

libs.forEach{lib->
    include(":$lib")
    project(":$lib").projectDir = new File(settingsDir, "./library/$lib")
}